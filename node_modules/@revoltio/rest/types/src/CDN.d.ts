export interface CDNOptions {
    api: string;
    cdn: string;
    app: string;
}
export declare class CDN {
    protected readonly options: CDNOptions;
    constructor(options: CDNOptions);
    defaultAvatar(id: string): string;
    avatar(hash: string, filename: string, size?: number): string;
    icon(hash: string, size?: number): string;
    invite(code: string): string;
    banner(hash: string, size?: number): string;
}
