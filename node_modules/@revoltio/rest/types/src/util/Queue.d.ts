interface DeferredPromise {
    resolve(): void;
    promise: Promise<void>;
}
export declare class Queue extends Array<DeferredPromise> {
    get remaining(): number;
    wait(): Promise<void>;
    next(): void;
}
export {};
