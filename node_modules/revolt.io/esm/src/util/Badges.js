import { BitField } from './BitField.js';
export class Badges extends BitField {
    constructor(bits) {
        super(bits);
    }
    static resolve(bit) {
        return super.resolve(bit);
    }
}
Object.defineProperty(Badges, "FLAGS", {
    enumerable: true,
    configurable: true,
    writable: true,
    value: {
        DEVELOPER: 1 << 0,
        TRANSLATOR: 1 << 1,
        SUPPORTER: 1 << 2,
        RESPONSIBLE_DISCLOSURE: 1 << 3,
        REVOLT_TEAM: 1 << 4,
        EARLY_ADOPTER: 1 << 8,
    }
});
