var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _ActionManager_actions;
import * as Actions from './mod.js';
export class ActionManager {
    constructor(client) {
        Object.defineProperty(this, "client", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: client
        });
        _ActionManager_actions.set(this, new Map());
        for (const Action of Object.values(Actions))
            this.register(Action);
    }
    register(Action) {
        __classPrivateFieldGet(this, _ActionManager_actions, "f").set(Action.name.replace(/Action$/, ''), new Action(this.client));
    }
    get(name) {
        return __classPrivateFieldGet(this, _ActionManager_actions, "f").get(name) ?? null;
    }
}
_ActionManager_actions = new WeakMap();
