import type { API } from '../../deps.js';
import { BaseManager } from './BaseManager.js';
import { Role, Server } from '../structures/mod.js';
export declare type RoleResolvable = Role | string;
export declare class RoleManager extends BaseManager<Role, API.Role & {
    id: string;
}> {
    protected readonly server: Server;
    holds: typeof Role;
    constructor(server: Server);
    _add(data: API.Role & {
        id: string;
    }): Role;
    create(name: string): Promise<Role>;
    delete(role: RoleResolvable): Promise<void>;
}
