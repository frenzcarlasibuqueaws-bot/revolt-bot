import type { API } from '../../deps.js';
import { BaseManager } from './BaseManager.js';
import { Server, ServerChannel } from '../structures/mod.js';
declare type APIServerChannel = Extract<API.Channel, {
    channel_type: 'TextChannel' | 'VoiceChannel';
}>;
export declare type ServerChannelResolvable = ServerChannel | APIServerChannel | string;
export interface CreateChannelOptions {
    name: string;
    type?: 'Text' | 'Voice';
    description?: string;
}
export declare class ServerChannelManager extends BaseManager<ServerChannel> {
    protected readonly server: Server;
    holds: typeof ServerChannel;
    constructor(server: Server);
    _add(data: API.Channel): ServerChannel;
    create({ name, type, description }: CreateChannelOptions): Promise<ServerChannel>;
    fetch(channel: ServerChannelResolvable, { force }?: {
        force?: boolean | undefined;
    }): Promise<ServerChannel>;
}
export {};
