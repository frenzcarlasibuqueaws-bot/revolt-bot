import type { API } from '../../deps.js';
import { BaseManager } from './BaseManager.js';
import { Message, User } from '../structures/mod.js';
export declare type UserResolvable = User | API.User | Message | string;
export declare class UserManager extends BaseManager<User, API.User> {
    holds: typeof User;
    fetch(user: UserResolvable, { force }?: {
        force?: boolean | undefined;
    }): Promise<User>;
    resolve(resolvable: Message | User): User;
    resolve(resolvable: string | API.User): User | null;
    resolveId(resolvable: UserResolvable): string | null;
}
