import type { API } from '../../deps.js';
import { BaseManager } from './BaseManager.js';
import { Channel } from '../structures/mod.js';
export declare type ChannelResolvable = Channel | API.Channel | string;
export declare class ChannelManager extends BaseManager<Channel, API.Channel> {
    holds: null;
    _add(data: API.Channel): Channel;
    _remove(id: string): void;
    delete(channel: ChannelResolvable): Promise<void>;
    fetch(channel: ChannelResolvable, { force }?: {
        force?: boolean | undefined;
    }): Promise<Channel>;
    resolve(channel: ChannelResolvable): Channel | null;
    resolveId(channel: ChannelResolvable): string | null;
}
