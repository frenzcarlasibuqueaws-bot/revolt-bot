import type { API } from '../../deps.js';
import { BaseClient, WebSocketShard } from './mod.js';
import { ActionManager } from './actions/ActionManager.js';
import { ChannelManager, ServerManager, UserManager } from '../managers/mod.js';
import type { ClientUser } from '../structures/mod.js';
export declare class Client extends BaseClient {
    protected readonly ws: WebSocketShard;
    readonly actions: ActionManager;
    readonly channels: ChannelManager;
    readonly servers: ServerManager;
    readonly users: UserManager;
    user: ClientUser | null;
    configuration?: API.RevoltConfig;
    readyAt: Date | null;
    get readyTimestamp(): number | null;
    get uptime(): number | null;
    login(token?: string, type?: 'user' | 'bot'): Promise<void>;
    destroy(): Promise<void>;
    isReady(): boolean;
}
export type { ClientOptions } from './BaseClient.js';
