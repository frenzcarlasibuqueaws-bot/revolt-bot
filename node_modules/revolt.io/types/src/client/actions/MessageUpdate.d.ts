import { Action, API } from './Action.js';
export declare class MessageUpdateAction extends Action {
    handle(data: {
        id: string;
        channel: string;
        data: API.Message;
    }): void;
}
