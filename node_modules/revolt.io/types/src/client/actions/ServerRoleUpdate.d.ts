import { Action, API } from './Action.js';
export declare class ServerRoleUpdateAction extends Action {
    handle(data: {
        id: string;
        role_id: string;
        data: API.Role & {
            _id: string;
        };
        clear: API.FieldsRole[];
    }): void;
}
