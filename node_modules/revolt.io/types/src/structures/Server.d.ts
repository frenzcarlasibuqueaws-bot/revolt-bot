import type { API } from '../../deps.js';
import { Attachment, Base, Category, ServerMember, User } from './mod.js';
import { Client } from '../client/Client.js';
import { RoleManager, ServerChannelManager, ServerMemberManager } from '../managers/mod.js';
import { Collection, ServerPermissions } from '../util/mod.js';
export declare class Server extends Base {
    name: string;
    description: string | null;
    ownerId: string;
    members: ServerMemberManager;
    channels: ServerChannelManager;
    roles: RoleManager;
    icon: Attachment | null;
    banner: Attachment | null;
    analytics: boolean;
    discoverable: boolean;
    nsfw: boolean;
    permissions: ServerPermissions;
    categories: Collection<string, Category>;
    constructor(client: Client, data: API.Server);
    protected _patch(data: API.Server, clear?: API.FieldsServer[]): this;
    get me(): ServerMember | null;
    get createdAt(): Date;
    get createdTimestamp(): number;
    get owner(): User | null;
    ack(): Promise<void>;
    delete(): Promise<void>;
    iconURL(options?: {
        size: number;
    }): string | null;
    bannerURL(options?: {
        size: number;
    }): string | null;
    toString(): string;
}
