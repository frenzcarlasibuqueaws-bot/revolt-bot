import type { API } from '../../deps.js';
import type { Client } from '../client/Client.js';
import type { MessageEditOptions } from '../managers/mod.js';
import { Base, DMChannel, Embed, GroupChannel, Mentions, Server, ServerMember, TextChannel, User } from './mod.js';
export declare class Message extends Base {
    type: Uppercase<API.SystemMessage['type']>;
    content: string;
    channelId: string;
    authorId: string;
    embeds: Embed[];
    attachments: API.File[];
    mentions: Mentions;
    editedTimestamp: number | null;
    constructor(client: Client, data: API.Message);
    protected _patch(data: API.Message): this;
    get createdAt(): Date;
    get createdTimestamp(): number;
    get editedAt(): Date | null;
    get system(): boolean;
    get author(): User | null;
    get channel(): TextChannel | DMChannel | GroupChannel;
    get serverId(): string | null;
    get server(): Server | null;
    get member(): ServerMember | null;
    get url(): string;
    ack(): Promise<void>;
    delete(): Promise<void>;
    reply(content: string, mention?: boolean): Promise<Message>;
    edit(options: MessageEditOptions | string): Promise<void>;
    fetch(): Promise<Message>;
    inServer(): this is this & {
        serverId: string;
        server: Server;
        channel: TextChannel;
    };
    toString(): string;
}
