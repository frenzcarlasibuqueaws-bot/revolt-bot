import type { API } from '../../deps.js';
import { Attachment, Base, DMChannel, Presence } from './mod.js';
import { Client } from '../client/Client.js';
import { Badges } from '../util/mod.js';
export declare class User extends Base {
    username: string;
    avatar: Attachment | null;
    presence: Presence;
    badges: Badges;
    bot: boolean;
    constructor(client: Client, data: API.User);
    protected _patch(data: API.User, clear?: API.FieldsUser[]): this;
    get createdAt(): Date;
    get createdTimestamp(): number;
    block(): Promise<void>;
    unblock(): Promise<void>;
    createDM(): Promise<DMChannel>;
    avatarURL(options?: {
        size: number;
    }): string | null;
    displayAvatarURL(options?: {
        size: number;
    }): string;
    fetch(force?: boolean): Promise<User>;
    toString(): string;
}
