import type { API } from '../../deps.js';
import { Base, Server, ServerChannel } from './mod.js';
import { Collection } from '../util/mod.js';
export declare class Category extends Base {
    readonly server: Server;
    name: string;
    protected _children: string[];
    constructor(server: Server, data: API.Category);
    protected _patch(data: API.Category): this;
    get children(): Collection<string, ServerChannel>;
    toString(): string;
}
