import type { API } from '../../deps.js';
import { Attachment, Category, Channel, Invite, Server } from './mod.js';
import { Client } from '../client/Client.js';
import { ChannelPermissions, Collection } from '../util/mod.js';
declare type APIServerChannel = Extract<API.Channel, {
    channel_type: 'TextChannel' | 'VoiceChannel';
}>;
export interface Overwrite {
    allow: ChannelPermissions;
    deny: ChannelPermissions;
}
export declare class ServerChannel extends Channel {
    name: string;
    serverId: string;
    description: string | null;
    icon: Attachment | null;
    overwrites: Collection<string, Overwrite>;
    nsfw: boolean;
    constructor(client: Client, data: APIServerChannel);
    protected _patch(data: APIServerChannel, clear?: API.FieldsChannel[]): this;
    createInvite(): Promise<Invite>;
    iconURL(options?: {
        size: number;
    }): string | null;
    get server(): Server;
    get category(): Category | null;
}
export {};
