import { Base } from './Base.js';
import type { API } from '../../deps.js';
import type { Server, User } from './mod.js';
import type { Client } from '../client/Client.js';
export declare class Invite extends Base {
    serverId: string | null;
    inviterId: string;
    channelId: string;
    constructor(client: Client, data: API.Invite);
    protected _patch(data: API.Invite): this;
    get server(): Server | null;
    get channel(): import("./Channel.js").Channel | null;
    get inviter(): User | null;
    toString(): string;
}
