import type { API } from '../../deps.js';
import { Message, ServerChannel } from './mod.js';
import { TextBasedChannel } from './interfaces/mod.js';
import { Client } from '../client/Client.js';
import { MessageManager, MessageOptions, MessageResolvable } from '../managers/mod.js';
import { ChannelTypes, Collection } from '../util/mod.js';
declare type APITextChannel = Extract<API.Channel, {
    channel_type: 'TextChannel';
}>;
export declare class TextChannel extends ServerChannel implements TextBasedChannel {
    lastMessageId: string | null;
    messages: MessageManager;
    readonly type = ChannelTypes.TEXT;
    constructor(client: Client, data: APITextChannel);
    protected _patch(data: APITextChannel): this;
    get lastMessage(): Message | null;
    send(options: MessageOptions | string): Promise<Message>;
    bulkDelete(messages: MessageResolvable[] | Collection<string, Message> | number): Promise<void>;
}
export {};
