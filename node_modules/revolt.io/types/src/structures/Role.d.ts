import type { API } from '../../deps.js';
import { Base, Overwrite, Server } from './mod.js';
export declare class Role extends Base {
    server: Server;
    name: string;
    color: string | null;
    hoist: boolean;
    rank: number;
    overwrite: Overwrite;
    constructor(server: Server, data: API.Role & {
        id: string;
    });
    protected _patch(data: API.Role & {
        _id?: string;
    }, clear?: API.FieldsRole[]): this;
    get createdAt(): Date;
    get createdTimestamp(): number;
    get permissions(): Overwrite;
    delete(): Promise<void>;
    toString(): string;
}
