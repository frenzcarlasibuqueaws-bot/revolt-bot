import { Message, ServerMember, User } from './mod.js';
import { Client } from '../client/Client.js';
import { UserResolvable } from '../managers/mod.js';
import { Collection } from '../util/mod.js';
export declare class Mentions {
    readonly message: Message;
    protected _users: string[];
    readonly client: Client;
    constructor(message: Message, _users: string[]);
    has(user: UserResolvable): boolean;
    get members(): Collection<string, ServerMember> | null;
    get users(): Collection<string, User>;
}
