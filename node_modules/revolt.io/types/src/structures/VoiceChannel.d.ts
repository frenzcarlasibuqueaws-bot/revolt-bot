import type { API } from '../../deps.js';
import { ServerChannel } from './mod.js';
import { Client } from '../lib.js';
import { ChannelTypes } from '../util/mod.js';
declare type APIVoiceChannel = Extract<API.Channel, {
    channel_type: 'VoiceChannel';
}>;
export declare class VoiceChannel extends ServerChannel {
    readonly type = ChannelTypes.VOICE;
    constructor(client: Client, data: APIVoiceChannel);
    protected _patch(data: APIVoiceChannel): this;
    ack(): Promise<void>;
}
export {};
