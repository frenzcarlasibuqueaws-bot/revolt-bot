import { Base } from './Base.js';
import type { DMChannel, GroupChannel, ServerChannel, TextChannel, VoiceChannel } from './mod.js';
import { ChannelTypes } from '../util/mod.js';
export declare abstract class Channel extends Base {
    type: ChannelTypes | 'UNKNOWN';
    get createdTimestamp(): number;
    get createdAt(): Date;
    delete(): Promise<void>;
    isText(): this is TextChannel | GroupChannel | DMChannel;
    isVoice(): this is VoiceChannel;
    isGroup(): this is GroupChannel;
    inServer(): this is ServerChannel;
    toString(): string;
    fetch(force?: boolean): Promise<Channel>;
}
